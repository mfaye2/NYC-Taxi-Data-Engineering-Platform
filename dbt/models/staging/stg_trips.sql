SELECT
    vendor_id,
    pickup_datetime,
    dropoff_datetime,
    passenger_count,
    trip_distance,
    pickup_location_id,
    dropoff_location_id,
    payment_type,
    fare_amount,
    tip_amount,
    tolls_amount,
    total_amount,
    pickup_borough,
    pickup_zone,
    dropoff_borough,
    dropoff_zone,
    trip_duration_minutes,
    average_speed_mph,
    pickup_date,
    pickup_year,
    pickup_month,
    pickup_day,
    pickup_hour,
    pickup_weekday,
    is_weekend,
    is_peak_hour,
    processing_timestamp

FROM {{ source('nyc_taxi_source', 'curated_trips') }}